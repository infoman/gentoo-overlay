# Commented lines will only work if ssl or passenger is installed
server {
	listen			80 default;
#	listen			443 default ssl;
#	passenger_enabled	on;
#	rails_env		development;
	root			/var/www/default/public;
	access_log		/var/www/default/log/access.log main;
	error_log		/var/www/default/log/error.log info;
}

# This will only work if you have ssl and rewrite are installed
#server {
#	listen			443 default;
#	ssl			on;
#	rewrite			^ http://$host/ permanent;
#}

